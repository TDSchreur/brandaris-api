trigger:
  - master

pr:
  - master

resources:
  - repo: self

variables:
  project: 'src/api/api.crproj'
  configuration: 'release'
  pool: Default

stages:
 - stage: Build
   jobs:
     - job: Build
       pool: $(pool)
       steps:
        - task: DotNetCoreCLI@2
          inputs:
            command: 'restore'
            projects: ${project}
            feedsToUse: 'select'
        - task: DotNetCoreCLI@2
          inputs:
            command: 'build'
            projects: ${project}
            arguments: '--no-restore -c ${configuration}'

        - task: DotNetCoreCLI@2
          inputs:
            command: 'publish'
            publishWebProjects: true
            arguments: '--no-build -c ${configuration}'
            buildProperties: 
            
        - task: PublishBuildArtifacts@1
          inputs:
            PathtoPublish: '$(Build.ArtifactStagingDirectory)'
            ArtifactName: 'drop'
            publishLocation: 'Container'